[project]
name = "xurrent-mcp"
version = "0.1.0"
description = "Local stdio MCP server exposing Xurrent (4me) API tools for Claude Code."
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.27,<2",
    "httpx>=0.27",
    "truststore>=0.9",
]

